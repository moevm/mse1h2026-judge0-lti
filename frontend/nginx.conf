server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
    # ! потом скорей всего убрать нужно будет
    location /api {
        proxy_pass http://backend-python-fastapi:8000;
    }
}